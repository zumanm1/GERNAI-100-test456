# These packages are for AI/LLM features and have conflicting dependencies with the core application.
# Install them in a separate environment or when you are ready to work on AI features.
# Updated to current working versions
openai==1.99.1
groq==0.31.0
crewai==0.157.0
langchain==0.3.27
langgraph  # Current version installed but not pinned
httpx==0.28.1
httpcore==1.0.9
openrouter>=0.1.0
chromadb==1.0.15
anthropic  # Current version installed but not pinned
